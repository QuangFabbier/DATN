{
  "name": "nexora-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev:fe",
    "dev:fe": "npm --prefix fe run dev",
    "dev:be": "npm --prefix be run dev",
    "build": "npm --prefix fe run build",
    "lint": "npm --prefix fe run lint",
    "preview": "npm --prefix fe run preview",
    "start:be": "npm --prefix be run start"
  }
}
